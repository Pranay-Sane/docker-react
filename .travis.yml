language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t pranaysane/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pranaysane/docker-react npm run test